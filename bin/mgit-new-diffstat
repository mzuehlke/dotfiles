#!/bin/bash

source `dirname $0`/../colors.bash
source `dirname $0`/../git.bash


function __do_new_diffstsat() {
    echo -e "${LIGHT_BLUE}${wd/$HOME/~}${NC}"
    git --no-pager diff --color=always --stat ..@{upstream}
}
foreach_gitdir __do_new_diffstsat $@ | git_pager
