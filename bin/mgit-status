#!/bin/bash

source `dirname $0`/../colors.bash
source `dirname $0`/../git.bash

while read line; do (
  cd $line;
  wd=$(pwd); 
  echo -e "${LIGHT_BLUE}${wd/$HOME/~}${NC} $(__git_ps1_mz)";
) done <$HOME/.mgitrepos
